/* This is only OK because in non-consumed expressions, we implicitly 
assume a linear primitive is being 'dot-accessed'. If this were a linear struct,
this test would fail.*/
linear int main() {
    
    linear int a;

    a = 5;

    a;

    return a;
} 
