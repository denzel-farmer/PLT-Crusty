struct lin_point {
    int x;
    int y;
};

char main() {
    print("Before");
    // This is fine, nothing gets silent discarded
    {struct lin_point -> 1,2};

    print("Done");

    return 'a';
} 
