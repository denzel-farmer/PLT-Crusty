
int main() {
    linear int a; 

    a = 1;
/* This is okay because a linear primitive in an expression that does not consume it 
implicitly treats that like a dot-access rather than a consumption/explosion */
    a;
    
    return a;
} 
